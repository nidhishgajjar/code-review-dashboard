# Orb Cloud API key — server-side only
ORB_API_KEY=orb_...

# GitHub PAT with public_repo — for looking up PR titles and comment URLs
GITHUB_TOKEN=ghp_...

# Agent deployments are configured in config/agents.json (committed to git).
# To rotate a CID: edit that file, commit, push. Vercel auto-deploys.
